# ⚠️ ARCHIVED — Render.com Deployment Notes

> **Status:** This document is no longer maintained.  
> The project now uses Docker Compose + the `infra/docker/` configuration for both local dev and production.  
> See `INSTALL.md` and `render.yaml` for current deployment options.

---

*Original content preserved below for historical reference only.*

